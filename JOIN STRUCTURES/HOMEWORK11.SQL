-- QUESTION 1
(SELECT FIRST_NAME FROM ACTOR)
    UNION
(SELECT FIRST_NAME FROM CUSTOMER);

-- QUESTION 2
(SELECT FIRST_NAME FROM ACTOR)
    INTERSECT
(SELECT FIRST_NAME FROM CUSTOMER);

-- QUESTION 3
(SELECT FIRST_NAME FROM ACTOR)
    EXCEPT
(SELECT FIRST_NAME FROM CUSTOMER);

-- QUESTION 4
(SELECT FIRST_NAME FROM ACTOR)
    UNION ALL
(SELECT FIRST_NAME FROM CUSTOMER);

(SELECT FIRST_NAME FROM ACTOR)
    INTERSECT ALL
(SELECT FIRST_NAME FROM CUSTOMER);

(SELECT FIRST_NAME FROM ACTOR)
    EXCEPT ALL
(SELECT FIRST_NAME FROM CUSTOMER);