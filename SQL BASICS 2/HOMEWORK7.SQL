-- QUESTION 1
SELECT RATING FROM DVDRENTAL.PUBLIC.FILM
    GROUP BY RATING;

-- QUESTION 2
SELECT REPLACEMENT_COST,COUNT(*) FROM DVDRENTAL.PUBLIC.FILM
    GROUP BY REPLACEMENT_COST
    HAVING COUNT(*) > 50;

-- QUESTION 3
SELECT STORE_ID,COUNT(*) FROM DVDRENTAL.PUBLIC.CUSTOMER
    GROUP BY STORE_ID;

-- QUESTION 4
SELECT COUNTRY_ID,COUNT(*) FROM DVDRENTAL.PUBLIC.CITY
    GROUP BY COUNTRY_ID
    ORDER BY COUNT(*) DESC
    LIMIT 1;